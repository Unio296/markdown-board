      <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
        <div class="container">
          <%= link_to root_path, id: "logo" ,class:"navbar-brand wf-nicomoji" do %>
            <i class="fa fa-inbox fa-lg"></i> Draft Box<span class="logo-dot">.</span>
          <% end %>


          <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarNav">

            <ul class="navbar-nav ml-auto">
              <% if logged_in? %>
              <li class="nav-item"><%= link_to current_user.nickname, user_path(current_user.nickname),class:"nav-link" %></li>
                <li class="nav-item dropdown show">
                    <%= image_tag current_user.image_url ,class:"nav-link dropdown-toggle", data:{toggle:"dropdown"} ,size:"50x50"%>
                    <ul class="dropdown-menu" role="menu">
                      <li><%= link_to 'マイページ', user_path(current_user.nickname), class:"dropdown-item" %></li>
                      <li><%= link_to 'ログアウト', logout_url, class:"dropdown-item"%></li>
                    </ul>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to 'Login', '/auth/twitter', class:"nav-link"%>
                </li>
              <% end %>
            </ul>

          </div>
        </div>
      </nav>  
    </header>