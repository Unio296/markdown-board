    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <%= link_to "Markdown Board", root_path, id: "logo" %>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <% if logged_in? %>
              <li class="nav-item">
                <p class="navbar-text"><%= current_user.name %></p>
              </li>
              <li class="nav-item">
                <%= link_to 'ログアウト', logout_path, class:"nav-link"%>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to 'ログイン', 'auth/twitter', class:"nav-link"%>
              </li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>